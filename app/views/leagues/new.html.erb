This will be the beginning of the league setup form

<%= simple_form_for @league do |f| %>
  <%= f.input :name, :placeholder => "Leage Name", required: true %>
  <%= f.input :url, :placeholder => "Input the subdomain you want for your league. EX: 'Alumni' becomes alumni.leaguehero.io", required: true %>
  <%= f.input :max_teams, required: true %>
  <%= f.input :max_players_per_team, required: true %>
<% end %>

<%= link_to('Next', new_user_registration_path) %>
