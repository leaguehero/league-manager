<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h2>Teams</h2>
      <table class="table teams_table">
        <thead>
          <tr>
            <th class="team_header">Team</th>
            <th class="team_header">Team Captain</th>
            <th></th>
          <% if current_user %>
            <th></th>
          <% end %>
          </tr>
        </thead>
        <tbody>
          <% @teams.each do |tm|%>
            <tr>
              <td>
                <%= tm.name  %>
              </td>
              <td class="team_captian">
                <%= !tm.captain.blank? ? tm.captain : "-" %>
              </td>
              <td>
                <a href="teams/<%= tm.id %>">View</a>
              </td>
            <% if current_user %>
              <td>
                <a href="teams/<%= tm.id %>/edit">Edit</a>
              </td>
            <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
